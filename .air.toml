[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "./tmp/main"
  dir = "."
  include_ext = ["go", "tpl", "html"]
  exclude_dir = ["vendor", "assets", "tmp"]
  poll = true
  poll_interval = 500

[log]
  level = "debug"